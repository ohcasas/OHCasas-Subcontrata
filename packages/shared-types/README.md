# packages/shared-types

Placeholder. Aquí irán los tipos TypeScript generados a partir del esquema
de Supabase (`supabase gen types typescript`) para compartir entre la app
móvil y cualquier función de servidor (Edge Functions), evitando duplicar
definiciones de las tablas de `supabase/migrations/0001_init_schema.sql`.
