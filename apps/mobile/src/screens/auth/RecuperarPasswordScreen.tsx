/**
 * Placeholder — Fase 0/scaffolding. Sin diseño ni lógica todavía.
 * Flujo de recuperación por email — pendiente
 */
import { View, Text } from 'react-native';

export default function RecuperarPasswordScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-surface">
      <Text className="text-onSurface font-sans text-titleMd">Recuperar contraseña</Text>
      <Text className="text-onSurfaceVariant font-sans text-bodySm mt-2">Pendiente de implementar</Text>
    </View>
  );
}
